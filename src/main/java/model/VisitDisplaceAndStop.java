package model;

import model.node.Node;

public class VisitDisplaceAndStop extends VisitRelocation {
    public VisitDisplaceAndStop(Node middleNode, Vehicle vehicle) {
        super(middleNode, vehicle);
    }
}
