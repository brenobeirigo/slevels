package model;

import model.node.Node;

public class VisitDisplaceAndRebalance extends VisitRelocation {
    public VisitDisplaceAndRebalance(Node middleNode, Vehicle vehicle) {
        super(middleNode, vehicle);
    }
}
